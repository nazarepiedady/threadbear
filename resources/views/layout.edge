<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">

    {{ style('bootstrap.min') }}
  </head>

  <body>
    <div class="container">
      <header class="row">
        <div class="col">
          @!section('header')
        </div>
      </header>

      <main class="row">
        <div class="col">
          @!section('content')
        </div>
      </main>
    </div>

    {{ script('jquery.min') }}
    {{ script('bootstrap.bundle.min') }}
  </body>
</html>
